import {
    Meteor
} from 'meteor/meteor';

import {
    HTTP
} from 'meteor/http'
import { console } from 'meteor/tools';

Meteor.methods({
    'searching' (location, service, date, time) {
        
    }
});